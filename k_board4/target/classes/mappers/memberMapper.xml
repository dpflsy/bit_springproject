<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kboard.mappers.memberMapper">

	<!-- register -->
	<insert id="register">
		insert into member(userid, userpw, phone,email)
		values(#{userid}, #{userpw}, #{phone},#{email})
	</insert>
	<!-- login -->
	<select id="login" resultType="com.kboard.domain.MemberVO">
		select userid,userpw,phone,email
		from
		member
		where userid =#{userid}
		and userpw=#{userpw}
	</select>
	<!-- modify -->
	<update id="modify">
		update member
		set
		userpw= #{userpw}
		where
		userid =
		#{userid}
	</update>
	<!-- delete -->
	<delete id="withdrawal">
		delete from member
		where userid=#{userid}
		and
		userpw=#{userpw}
	</delete>
	<!-- 아이디 확인 -->
	<select id="idCheck" resultType="com.kboard.domain.MemberVO">
		select userid from member
		where userid = #{userid}
	</select>
</mapper>